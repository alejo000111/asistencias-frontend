# ============================================================
# 🖥️ ENTORNO DE DESARROLLO LOCAL (npm run dev)
# ============================================================
# Vacío a propósito: Axios usa rutas relativas (/api/...)
# y el proxy de Vite (vite.config.js) las redirige a localhost:8080.
#
# Ver: src/main.js — import.meta.env.DEV evita setear baseURL.
# ============================================================

